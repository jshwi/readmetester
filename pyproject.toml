[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]

[tool.black]
exclude = '''
/(
  | \.git
  | \.mypy_cache
  | _build
  | build
  | dist
)/
'''
line-length = 79

[tool.coverage.run]
omit = ["whitelist.py", "readmetester/__main__.py"]

[tool.poetry]
authors = ["jshwi <stephen@jshwisolutions.com>"]
description = "Parse, test, and assert RST code-blocks"
license = "MIT"
name = "readmetester"
version = "1.0.1"
readme = "README.rst"

[tool.poetry.dependencies]
Pygments = "^2.11.2"
object-colors = "^2.0.1"
python = "^3.8"

[tool.poetry.dev-dependencies]
bump2version = "^1.0.1"
ipython = "^8.1.1"
pyaud = "^3.9.0"
pytest = "^6.2.5"
restview = "^3.0.0"
toml-sort = "^0.19.0"

[tool.poetry.scripts]
readmetester = "readmetester.__main__:main"

[tool.pytest.ini_options]
addopts = [
    "--durations=5",
     "-vv",
     "--color=yes",
     "--cov-report",
     "term-missing"
]
filterwarnings = "ignore::DeprecationWarning"
norecursedirs = ["docs", "*.egg-info", ".git", ".mypy_cache", ".idea", "prof"]
